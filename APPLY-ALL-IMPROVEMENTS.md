# APPLY ALL IMPROVEMENTS — master directive (do this across every unit)

The single, complete work order. Apply to the **existing Unit 1 & 2 decks now**, and **bake into every remaining unit** (3–12 + the 4 exam papers) as they're built. **All content is already authored and verified in this project — place it, don't regenerate it.** After editing any deck, `eval_js` that **every `<section>` measures `scrollHeight ≤ 1080`** and that no answer leaks on its question slide.

## Where the content lives
| Content | Units 1–2 | Units 3–12 |
|---|---|---|
| Grammar practice | `data/grammar-extra-practice.md` | `data/grammar-extra-practice-units-3-12.md` |
| Reading glossaries + vocab tasks | `data/reading-glossaries-and-vocab-tasks.md` | `data/reading-glossaries-units-3-12.md` |
| Graphic organisers + Support/Stretch | (worked examples in the scaffolds file below) | `data/scaffolds-units-3-12.md` |
| Colour fix · writing weak-vs-strong · vocab spiral · joins | `data/scaffolds-writing-vocab-and-fixes.md` (templates apply to every unit) | ← same |

## Order of work (per deck)
1. **Global colour fix** (`data/scaffolds-writing-vocab-and-fixes.md` §P1.1): annotation → **bordered rectangular chips** Meaning `#dcebe9`/`#2f6d6a` · Method `#f4e2d4`/`#b4632e` · Response `#efe1ee`/`#8a5a86`; leave the round mode badges and green answer-reveal. Do this in **every** deck.
2. **Reading lessons:** add the passage's **tricky-words strip** + **"find the word…"** task from the glossary file for that unit (match by passage title). Add the passage's **graphic organiser** + **Support/Stretch** from `data/scaffolds-units-3-12.md` (Units 1–2 use the worked examples in the scaffolds-and-fixes file).
3. **Grammar lessons:** add the four-part practice (Spot-Error / Transform / Gap-Fill / Build-Your-Own) from the grammar file for that unit (match by grammar focus). Keys stay teacher-only, Q→A reveal.
4. **All lessons:** tier tasks Support/Core/Stretch; sentence frames on discussion/vote slides; L1-bridge box on the 2–3 hardest concepts (§P2).
5. **Writing lessons:** add the weak→strong model pair + "avoid these" box (§P3). **Vocab/Do-Now:** add a cross-unit spiral recall + a word-family extension (§P3).
6. **Close every lesson:** exit ticket → one-line bridge to that night's packet task; objectives rewritten as **"I can…"** ticks (§P4).
7. **Polish (optional §P5):** footer progress cue; a pronunciation-clinic slide in vocab-heavy lessons.

## Sequencing
- **Pass A (now):** steps 1–6 on the built Unit 1 & 2 decks + packets.
- **Pass B (ongoing):** as you build each remaining unit, apply steps 1–6 using that unit's pre-authored files above (place the content; only match it to the deck's passages/grammar focus).

## Notes for a teacher's eye
- Grammar keys were verified item-by-item. One judgement point: U12 grammar gap-fill gap 2 (*we should stay calm* — natural; *had to* also works).
- `data/scaffolds-units-3-12.md` scaffolded Unit 10's **environment strand** as its core; if that unit's focus is its "welcoming newcomers" strand instead, that set wants a quick re-point.
- The glossary set covers 92 passages (all student texts, backups/exam-fragments filtered); use the entries whose titles match your decks' readings.
